services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hiverunr
      POSTGRES_USER: hiverunr
      POSTGRES_PASSWORD: hiverunr
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL","pg_isready -U hiverunr"]
      interval: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD","redis-cli","ping"]
      interval: 5s
      retries: 10

  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health'); print('ok')\""]
      interval: 5s
      retries: 12
      start_period: 20s
    volumes:
      - ./app:/app/app
      # The Vite build lives at /app/frontend_dist/ inside the Docker image.
      # This path is NOT covered by the ./app bind mount above, so the compiled
      # assets are always available regardless of what's on the host filesystem.
      - ./migrations:/app/migrations
      - ./alembic.ini:/app/alembic.ini
      - ./runlogs:/app/runlogs
    env_file: .env
    depends_on:
      db: {condition: service_healthy}
      redis: {condition: service_healthy}

  worker:
    build: .
    command: celery -A app.worker worker --loglevel=info --concurrency=4
    volumes:
      - ./app:/app/app
      - ./migrations:/app/migrations
      - ./alembic.ini:/app/alembic.ini
      - ./runlogs:/app/runlogs
    env_file: .env
    depends_on:
      db: {condition: service_healthy}
      redis: {condition: service_healthy}

  scheduler:
    build: .
    command: python -m app.scheduler
    volumes:
      - ./app:/app/app
      - ./migrations:/app/migrations
      - ./alembic.ini:/app/alembic.ini
    env_file: .env
    depends_on:
      db: {condition: service_healthy}
      redis: {condition: service_healthy}

  flower:
    build: .
    command: celery -A app.worker flower --port=5555 --url_prefix=flower
    env_file: .env
    depends_on:
      - redis

  caddy:
    image: caddy:2-alpine
    ports:
      - "${CADDY_HTTP_PORT:-80}:80"
      - "${CADDY_HTTPS_PORT:-443}:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api
      - flower

volumes:
  pg_data:
  caddy_data:
  caddy_config:
