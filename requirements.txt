alembic>=1.13.0,<1.14.0
slowapi==0.1.9
opentelemetry-api>=1.24.0,<1.27.0
# Optional: install the SDK + OTLP exporter to enable distributed tracing
# (set OTEL_EXPORTER_OTLP_ENDPOINT in .env — zero-overhead when unset, only api needed)
# opentelemetry-sdk>=1.24.0
# opentelemetry-exporter-otlp-proto-http>=1.24.0
fastapi>=0.115.0,<0.137.0
prometheus_client>=0.20.0,<1.0.0
python-json-logger>=2.0.7,<5.0.0
uvicorn[standard]==0.29.0
celery==5.4.0
redis==5.0.4
psycopg2-binary==2.9.9
flower==2.0.1
python-multipart>=0.0.22,<1.0.0
pydantic==2.7.1
httpx==0.27.0
apscheduler>=3.10.4,<4.0.0
openai>=1.30.0,<2.38.0
paramiko>=5.0.0
google-auth>=2.52.0,<3.1.0
bcrypt==4.1.3
cryptography>=46.0.6,<48.0.0
starlette>=0.47.2,<1.1.0
# Optional: install boto3 to use SECRETS_PROVIDER=aws (AWS Secrets Manager)
# boto3>=1.34.0
# Optional: install xhtml2pdf to use the action.pdf node (requires system cairo libs on some platforms)
# xhtml2pdf>=0.2.16
